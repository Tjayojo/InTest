## Build output
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/
artifacts/

## Visual Studio / Rider / VS Code
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json

## Test results
[Tt]est[Rr]esult*/
*.trx
*.coverage
*.coveragexml
coverage.json
coverage.cobertura.xml
coverage.opencover.xml
coverage.info
# NOT ignored: coverage-report.json — InTest's own report is committed and --check'd (§8, §12)
BenchmarkDotNet.Artifacts/

## NuGet
*.nupkg
*.snupkg
**/[Pp]ackages/*
!**/[Pp]ackages/build/
.nuget/

## Local configuration and secrets — never commit
appsettings.local.json
appsettings.*.local.json
*.local.json
secrets.json
*.pfx
*.snk

## OS
.DS_Store
Thumbs.db

## Git worktrees — isolated workspaces, never repository content
.worktrees/

## Misc
*.tmp
*.bak
*.swp

## Agent instruction files — local tooling, deliberately not published.
## Kept on disk, untracked. Not part of the project; do not re-add without discussion.
## CLAUDE.md is the deliberate exception: the user decided it should ship, and it is tracked.
AGENTS.md
GEMINI.md
caveman.*
.clinerules/
.cursor/
.opencode/
.windsurf/
.github/copilot-instructions.md
## Sample API databases — recreated and seeded on startup
samples/**/*.db
samples/**/*.db-shm
samples/**/*.db-wal

# Identity.Server generates a dev signing key on first run; it is per-machine, not shared.
samples/Identity.Server/tempkey.jwk

# logs
logs/
*.log
